[pytest]
testpaths = tests
# Show short test summary info for failures and skips
addopts = -v --tb=short -rfs
# Register custom markers so pytest doesn't warn about unknown marks
markers =
    gpu: requires a CUDA-capable GPU (auto-skipped on CPU-only machines)
